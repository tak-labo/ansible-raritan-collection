[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "raritan-xerus-tests"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "raritan>=4.3.0",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
